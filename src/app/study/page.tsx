import { PlatformPlaceholder } from "@/features/platform/platform-route-shell";

export default function StudyPage() {
  return (
    <PlatformPlaceholder
      description="QuesIQ Study will be imported here as its own product lane, with shared sign-in and Study-owned content, progress, and AI surfaces."
      eyebrow="QuesIQ Study"
      title="Study"
    />
  );
}
