# Strategy Docs

This folder holds future-facing plans that are useful for direction setting but
are not yet active implementation requirements.

Strategy docs should become current product or architecture work only after the
decision is reflected in `docs/rebuild/DECISIONS.md` or the active handoff/status
docs.
